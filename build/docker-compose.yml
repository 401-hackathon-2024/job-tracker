services:
  frontend:
    build: "../src/."
    ports:
      - "80:3000"
  backend:
    build: "../src-django/."
    ports:
      - "8000:8000"
  extension:
    build: "../src-ext/."
    ports:
      - "4000:4000"
